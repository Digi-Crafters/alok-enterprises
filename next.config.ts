import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com","thevoxsolar.com","imgs.search.brave.com"],
  }
};

export default nextConfig;
